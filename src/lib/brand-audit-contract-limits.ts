// SPDX-License-Identifier: BUSL-1.1

/** Shared producer and webhook-contract bound for candidate diff collections. */
export const BRAND_AUDIT_WATCH_CHANGE_ROWS_MAX = 200;

/** Maximum UTF-8 encoded Brand Drift webhook request body. */
export const BRAND_AUDIT_WATCH_WEBHOOK_MAX_BODY_BYTES = 256 * 1024;
