startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: API Key
        description: >-
          Optional API key for higher rate limits and premium features.
          Leave blank to use the free tier (50 req/min).
    required: []
  url: https://dns-mcp.blackveilsecurity.com/mcp
